source 'https://rubygems.org'

ruby '2.2.0'
gem 'rails', '4.0.13'
gem 'mongoid', git: 'https://github.com/mongoid/mongoid.git'

# Used to generate brackets for individual events
gem 'bracket_tree'

# Configure environment variables via config/application.yml
gem 'figaro'

# Used for generating random "proof of score" strings of words/numbers
gem 'forgery'

# Use jquery as the JavaScript library
gem 'jquery-rails'

# User for uploading images
gem 'cloudinary'

# Email management via Mandrill
gem 'mandrill-api'

# Autocomplete for searching
gem 'rails3-jquery-autocomplete'

# Use devise for authentication
gem 'devise'

# Use Foundation for layouts
gem 'foundation-rails'

# Use HAML
gem "haml-rails"

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.2'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use Foundation icon font
gem 'foundation-icons-sass-rails'

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
# gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.1.2'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
